# 彩票店记账系统 - 后端

FROM python:3.12-slim

WORKDIR /app

# 安装 uv（轻量级 Python 包管理器）
RUN pip install --no-cache-dir uv

# 复制后端代码（含 pyproject.toml）
COPY backend/ .

# 安装依赖
RUN uv sync --frozen --no-install-project

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
